# WPS密码管理插件 - 项目文档

## 1. 项目概述

### 1.1 项目定位

本项目是一款面向企业用户的WPS Office文档密码管理插件，旨在实现文档加密/解密过程的自动化和安全化。插件通过UI自动化技术检测WPS密码对话框，自动填充密码或生成强密码，并将密码信息安全存储在文档元数据中，实现文档密码的全生命周期管理。

### 1.2 核心功能

| 功能模块    | 功能描述                      |
| ------- | ------------------------- |
| 文档解密自动化 | 检测加密文档打开时的密码输入框，自动填充密码    |
| 文档加密自动化 | 检测文档加密对话框，提供一键生成强密码功能     |
| 密码安全存储  | 将加密后的密码存储在文档ZIP尾部的自定义元数据中 |
| 用户认证管理  | 支持用户登录、注销、Token刷新及心跳检测    |
| 文档权限管理  | 支持查询和更新文档访问权限（读/写权限）      |
| 操作审计日志  | 记录所有关键操作，支持日志查看和导出        |

### 1.3 适用场景

- 企业内部文档加密管理，统一密码策略
- 文档权限控制，精细化访问管理
- 自动化解密流程，提升办公效率
- 密码安全审计，满足合规要求

## 2. 架构设计

### 2.1 分层架构

系统采用**四层分层架构**，各层职责明确，依赖关系清晰：

```
┌─────────────────────────────────────────────────────────────┐
│                   Presentation Layer (UI)                   │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │ TrayIcon    │ │ LoginForm   │ │ FloatingButton      │   │
│  │ AuthTreeForm│ │ LogForm     │ │ NotificationForm    │   │
│  │ MetaQueryForm│              │ │ FloatingButtonManager│  │
│  └─────────────┘ └─────────────┘ └─────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                            ▲
                            │ 依赖
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   Business Logic Layer                      │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │FileMeta     │ │FileMeta     │ │PasswordGenerator   │   │
│  │Factory      │ │Manager      │ │AutoFillAttempt     │   │
│  └─────────────┘ └─────────────┘ │Manager             │   │
│  ┌─────────────┐ ┌─────────────┐ └─────────────────────┘   │
│  │AutoFill     │ │WpsMonitor   │ ┌─────────────────────┐   │
│  │Manager      │ │             │ │OfficeEncryptUtils  │   │
│  └─────────────┘ └─────────────┘ │ZipExtraFieldManager│   │
│  ┌─────────────┐ ┌─────────────┐ │FileStateManager    │   │
│  │QtWindow     │ │Password     │ └─────────────────────┘   │
│  │Locator      │ │AutoFiller   │                          │
│  └─────────────┘ └─────────────┘                          │
└─────────────────────────────────────────────────────────────┘
                            ▲
                            │ 依赖
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                      Service Layer                          │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │HttpRequest  │ │ Request     │ │PasswordReport      │   │
│  │Service      │ │ Factory     │ │Service             │   │
│  └─────────────┘ └─────────────┘ └─────────────────────┘   │
│  ┌─────────────┐ ┌─────────────┐                           │
│  │ApiRoutes    │ │ApiResponse  │                           │
│  └─────────────┘ └─────────────┘                           │
└─────────────────────────────────────────────────────────────┘
                            ▲
                            │ 依赖
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   Utility Layer (Utils)                     │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │GlobalState  │ │Storage      │ │Logger               │   │
│  │             │ │Manager      │ │                     │   │
│  └─────────────┘ └─────────────┘ └─────────────────────┘   │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────────┐   │
│  │CryptoUtils  │ │DpiHelper    │ │DynamicHttpClient    │   │
│  │             │ │             │ │Manager              │   │
│  └─────────────┘ └─────────────┘ └─────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责划分

#### 2.2.1 表示层（UI Layer）

**职责**：负责用户交互界面的展示和用户输入的收集，所有UI操作不阻塞后台监控线程。

**核心组件**：

| 组件名称                  | 类型    | 职责描述                                 |
| --------------------- | ----- | ------------------------------------ |
| TrayIcon              | 系统托盘  | 系统托盘图标管理、右键菜单（主页、显示日志、打开目录、查询元数据、退出） |
| LoginForm             | 登录窗口  | 用户认证界面、服务器配置、登录/注销逻辑、心跳检测            |
| FloatingButton        | 悬浮按钮  | 密码框旁的操作按钮组（生成密码、提取密码、文档权限）           |
| FloatingButtonManager | 按钮管理器 | 通过命令队列和后台线程管理悬浮按钮的显示/隐藏/位置更新         |
| AuthTreeForm          | 权限树表单 | 文档权限树展示和编辑，支持部门和员工权限配置               |
| LogForm               | 日志表单  | 实时显示日志内容，支持暂停/恢复/清空/加载日志文件           |
| MetaQueryForm         | 元数据查询 | 管理员专用，查询文件的UID、加密密码、密钥版本、解密密码        |
| NotificationForm      | 通知表单  | 轻量提示框，自动关闭机制（默认3秒）                   |

#### 2.2.2 业务逻辑层（Business Layer）

**职责**：实现核心业务逻辑，包括窗口监控、密码生成、文档元数据管理、文件状态检测等。

**核心组件**：

| 组件名称                   | 类型        | 职责描述                                                        |
| ---------------------- | --------- | ----------------------------------------------------------- |
| WpsMonitor             | WPS监控器    | WPS进程监控、密码对话框识别（标题匹配："文档已加密"、"密码加密"）、文档路径获取、窗口定位            |
| QtWindowLocator        | Qt窗口定位器   | Qt框架窗口的子控件定位，支持多级搜索策略（直接搜索、进程窗口枚举、扩展搜索）                     |
| PasswordAutoFiller     | 密码填充器     | 通过UI Automation技术实现密码填充和按钮点击                                |
| FileMeta               | 文件元数据     | 存储文件路径、UID、当前密码、待定密码列表、所有者信息、读写权限、密钥版本                      |
| FileMetaFactory        | 元数据工厂     | 单例模式管理FileMeta对象，提供获取、添加、更新、清理、等待初始化等操作                     |
| FileMetaManager        | 元数据管理器    | 文件元数据的读写操作，包括密码（类型1）、UID（类型2）、密钥版本（类型3）                     |
| OfficeEncryptUtils     | 加密工具      | Office文档加密状态检测、密码验证（支持OLE格式和Open XML格式）                     |
| PasswordGenerator      | 密码生成器     | 生成符合安全规范的强密码（10位，包含大小写字母、数字、特殊符号各2位）                        |
| AutoFillAttemptManager | 填充管理器     | 自动填充尝试记录，防止重复填充，支持重置和清理                                     |
| FileStateManager       | 文件状态管理器   | 文件特征（修改时间、哈希值）登记和检测，用于判断文件是否被修改                             |
| ZipExtraFieldManager   | ZIP元数据管理器 | 在ZIP文件尾部读写自定义元数据块，包含Magic、Version、Type、Length、Data、Checksum |

#### 2.2.3 服务层（Service Layer）

**职责**：封装HTTP通信、API路由、报告生成等远程服务能力。

**核心组件**：

| 组件名称                  | 类型     | 职责描述                                 |
| --------------------- | ------ | ------------------------------------ |
| HttpRequestService    | HTTP服务 | 封装GET/POST/PUT请求，支持查询参数、请求头、响应反序列化   |
| RequestFactory        | 请求工厂   | HTTP请求服务的单例获取                        |
| ApiRoutes             | 路由常量   | 后端API路由定义（登录、登出、刷新Token、文档密码、权限树等）   |
| ApiResponse           | 响应模型   | 统一API响应格式（status、message、data）及数据类定义 |
| PasswordReportService | 报告服务   | 密码使用情况上报，使用ECC+AES混合加密密码后上报至服务器      |

#### 2.2.4 工具层（Utility Layer）

**职责**：提供全局状态管理、本地存储、日志记录、加密工具等通用能力。

**核心组件**：

| 组件名称                     | 类型        | 职责描述                                       |
| ------------------------ | --------- | ------------------------------------------ |
| GlobalState              | 全局状态      | 单例模式的用户信息、配置、登录状态、公钥、密钥版本、可能路径缓存           |
| StorageManager           | 存储管理      | 本地配置、用户信息、密钥信息的读写，使用AES加密敏感数据              |
| Logger                   | 日志记录      | 分级日志（Debug/Info/Warning/Error），异步写入文件和日志窗口 |
| CryptoUtils              | 加密工具      | 使用ECC公钥加密密码（ECDH密钥协商 + AES-CBC加密）          |
| DpiHelper                | DPI助手     | DPI感知初始化、缩放计算、控件缩放适配                       |
| DynamicHttpClientManager | HTTP客户端管理 | 动态创建HTTP客户端，支持自定义超时和证书验证                   |

### 2.3 层次依赖关系

- **上层可依赖下层，下层不可依赖上层**
- **同层之间通过接口抽象进行松耦合**
- **禁止跨层直接依赖**

### 2.4 关键设计模式

| 设计模式      | 应用场景              | 实现类                                               |
| --------- | ----------------- | ------------------------------------------------- |
| 单例模式      | 全局状态管理、元数据工厂、报告服务 | GlobalState、FileMetaFactory、PasswordReportService |
| 观察者模式     | 登录/注销事件通知         | LoginForm.LoginSuccess、LoginForm.LogoutSuccess    |
| 工厂模式      | HTTP请求服务创建        | RequestFactory                                    |
| 生产者-消费者模式 | 日志写入、命令队列         | Logger、FloatingButtonManager                      |

## 3. 技术栈说明

### 3.1 核心技术栈

| 分类        | 技术                     | 版本     | 用途                  |
| --------- | ---------------------- | ------ | ------------------- |
| 开发框架      | .NET                   | 6.0    | Windows Desktop应用开发 |
| 编程语言      | C#                     | 10.0   | 核心业务逻辑实现            |
| UI框架      | Windows Forms          | -      | 用户界面开发              |
| UI自动化     | UI Automation          | -      | 密码对话框识别和填充          |
| Win32 API | User32.dll             | -      | 窗口枚举、定位、控制          |
| Office处理  | NPOI                   | 2.6.0  | OLE格式文档加密检测和密码验证    |
| Open XML  | DocumentFormat.OpenXml | 2.20.0 | Open XML格式文档处理      |
| JSON序列化   | Newtonsoft.Json        | 13.0.4 | API响应序列化            |
| 日志        | -                      | 自定义    | 分级日志系统              |
| HTTP客户端   | HttpClient             | -      | RESTful API调用       |

### 3.2 加密技术

| 技术      | 用途     | 实现细节                  |
| ------- | ------ | --------------------- |
| ECC     | 密码加密传输 | 使用nistP256曲线，ECDH密钥协商 |
| AES-CBC | 密码本地存储 | PKCS7填充，固定IV（零向量）     |
| CRC32   | 元数据校验  | 确保元数据完整性              |

### 3.3 支持的文档格式

- `.docx` - Word文档
- `.xlsx` - Excel文档
- `.pptx` - PowerPoint文档

## 4. 核心功能模块详解

### 4.1 WPS监控模块

#### 4.1.1 WpsMonitor

**核心功能**：检测WPS进程运行状态、识别密码对话框、获取文档路径。

**关键方法**：

| 方法                     | 功能描述                          |
| ---------------------- | ----------------------------- |
| `IsWpsRunning()`       | 检查WPS进程是否运行                   |
| `FindPasswordDialog()` | 查找密码对话框（支持"文档已加密"和"密码加密"两种标题） |
| `GetDocumentName()`    | 通过窗口标题获取文档名称                  |
| `GetDocumentPath()`    | 通过文档名称匹配可能路径，支持缓存             |
| `GetDpiScale()`        | 获取窗口所在显示器的DPI缩放比例             |
| `GetWindowRect()`      | 获取窗口矩形区域                      |

**对话框识别策略**：

1. **直接搜索**：通过标题和类名组合查找（Qt5QWindow、Qt5Dialog、#32770、QDialog、QWidget）
2. **进程窗口枚举**：枚举WPS进程的所有窗口，匹配标题
3. **扩展搜索**：枚举所有可见窗口，匹配标题并验证是否为Qt窗口和WPS进程

### 4.2 密码自动填充模块

#### 4.2.1 PasswordAutoFiller

**核心功能**：通过UI Automation技术实现密码填充和按钮点击。

**关键方法**：

| 方法                              | 功能描述                            |
| ------------------------------- | ------------------------------- |
| `FillDecryptPassword()`         | 填充解密密码（针对"文档已加密"对话框）            |
| `FillEncryptPassword()`         | 填充加密密码（针对"密码加密"对话框，支持双密码框）      |
| `FillPasswordByUIAutomation()`  | 核心填充逻辑，通过反射动态加载UI Automation程序集 |
| `ClickOkButtonByUIAutomation()` | 通过UI Automation点击确定按钮           |

**UI Automation加载策略**：

- 优先加载带版本号的程序集（Version=4.0.0.0）
- 失败时尝试加载无版本号的程序集
- 使用反射获取类型和方法，避免编译时依赖

### 4.3 元数据管理模块

#### 4.3.1 FileMeta

**数据结构**：

```csharp
public class FileMeta
{
    public string FilePath;                // 文件绝对路径
    public string Uid;                    // 文件权限标识（时间戳_GUID格式）
    public string CurrentPassword;        // 当前生效的密码
    public string AfterPassword;          // 待写入的新密码
    public SortedSet<string> PendingPasswordList; // 待定密码集合
    public string OwnerAccount;           // 文档所属账号
    public string OwnerName;              // 文档所属名称
    public bool ReadAuth;                 // 读权限
    public bool WriteAuth;                // 写权限
    public string CurrentKeyVersion;      // 当前密钥版本（默认"default"）
    public bool IsWriting;                // 是否正在写入元数据（防止重复写入）
}
```

#### 4.3.2 FileMetaFactory

**核心功能**：单例管理FileMeta对象，支持线程安全的元数据操作。

**关键方法**：

| 方法                        | 功能描述                |
| ------------------------- | ------------------- |
| `GetFileMeta()`           | 获取文件元数据             |
| `AddFileMeta()`           | 添加文件元数据             |
| `UpdatePendingPassword()` | 更新待定密码（最多保留5个）      |
| `GetWritePassword()`      | 获取用于写入的密码（优先验证待定密码） |
| `UpdateCurrentPassword()` | 更新当前密码              |
| `WaitForInit()`           | 等待元数据初始化完成（最多5秒）    |
| `SignalInitComplete()`    | 发出初始化完成信号           |
| `CleanupFileMeta()`       | 清理文件元数据             |

#### 4.3.3 FileMetaManager

**核心功能**：文件元数据的持久化读写。

**元数据类型定义**：

| 类型值 | 元数据类型      | 说明     |
| --- | ---------- | ------ |
| 1   | Password   | 加密后的密码 |
| 2   | UID        | 文件唯一标识 |
| 3   | KeyVersion | 密钥版本   |

**密码加密流程**：

1. 使用ECC公钥进行ECDH密钥协商
2. 生成AES密钥（SHA256哈希派生）
3. 使用AES-CBC模式加密密码
4. 组合格式：\[临时公钥长度(4)]\[临时公钥(N)]\[IV(16)]\[密文(M)]

#### 4.3.4 ZipExtraFieldManager

**核心功能**：在ZIP文件尾部读写自定义元数据块。

**元数据块格式**：

```
[Magic(4)] [Version(2)] [Type(1)] [Length(4)] [Data(N)] [Checksum(4)]
```

| 字段       | 长度  | 说明                             |
| -------- | --- | ------------------------------ |
| Magic    | 4字节 | "WPPM"（WPS Password Manager标识） |
| Version  | 2字节 | 元数据格式版本（当前为1）                  |
| Type     | 1字节 | 元数据类型（1=密码，2=UID，3=密钥版本）       |
| Length   | 4字节 | 数据长度                           |
| Data     | N字节 | 实际数据                           |
| Checksum | 4字节 | CRC32校验和                       |

### 4.4 密码生成模块

#### 4.4.1 PasswordGenerator

**密码规范**：

| 字符类型 | 数量要求 | 字符集       |
| ---- | ---- | --------- |
| 大写字母 | 至少2位 | A-Z       |
| 小写字母 | 至少2位 | a-z       |
| 数字   | 至少2位 | 0-9       |
| 特殊符号 | 至少2位 | !@#$%^&\* |
| 总长度  | 10位  | -         |

**生成算法**：

1. 确保每个类别至少2个字符
2. 填充剩余位置（从所有字符集中随机选取）
3. 检查连续字符（避免相同字符连续出现）
4. 检查相似字符（避免1/l、0/O、I/l混淆）
5. 使用随机排序打乱字符顺序

### 4.5 用户认证模块

#### 4.5.1 LoginForm

**核心功能**：用户登录、注销、服务器配置、心跳检测。

**认证流程**：

1. 用户输入用户名、密码、域名、端口
2. 校验输入格式（域名必须使用HTTPS）
3. 调用 `/account/login` 接口登录
4. 保存Token到本地存储（AES加密）
5. 登录成功后启动监控线程
6. 心跳检测通过 `/account/refresh-token` 接口实现

**注销流程**：

1. 检测WPS是否运行（建议先关闭WPS）
2. 调用 `/account/logout` 接口注销
3. 清理本地用户信息和资源
4. 停止监控线程

### 4.6 悬浮按钮模块

#### 4.6.1 FloatingButton

**核心功能**：在密码对话框旁显示操作按钮组。

**按钮功能**：

| 按钮   | 功能描述                       | 触发条件        |
| ---- | -------------------------- | ----------- |
| 生成密码 | 生成强密码并填充到加密对话框             | 加密对话框 + 写权限 |
| 提取密码 | 通过UI Automation获取密码并复制到剪贴板 | 加密对话框       |
| 文档权限 | 打开权限配置窗口                   | 加密对话框 + 写权限 |

**显示策略**：

- 仅在"密码加密"对话框中显示（解密对话框不显示）
- 根据文件元数据的WriteAuth属性决定是否显示"文档权限"按钮
- 按钮位置跟随对话框右侧，垂直居中
- 支持DPI缩放适配

#### 4.6.2 FloatingButtonManager

**核心功能**：通过命令队列和后台线程管理悬浮按钮。

**命令类型**：

| 命令             | 功能描述    |
| -------------- | ------- |
| Show           | 显示悬浮按钮  |
| Hide           | 隐藏悬浮按钮  |
| UpdatePosition | 更新按钮位置  |
| UpdateFileMeta | 更新文件元数据 |
| Exit           | 退出管理器   |

**线程模型**：

- 使用SynchronizationContext确保UI操作在主线程执行
- 使用BlockingCollection实现命令队列
- 后台线程处理命令，避免阻塞UI

### 4.7 文件状态检测模块

#### 4.7.1 FileStateManager

**核心功能**：通过文件特征检测判断文件是否被修改。

**文件特征**：

| 特征            | 用途       |
| ------------- | -------- |
| LastWriteTime | 文件最后修改时间 |
| SHA256 Hash   | 文件内容哈希值  |

**检测流程**：

1. 文档打开时登记文件特征
2. 定时检测文件修改时间是否变化
3. 如果修改时间变化，重新计算哈希值
4. 比较哈希值判断文件内容是否真正修改
5. 如果内容修改，触发元数据写入逻辑

## 5. 关键业务流程

### 5.1 文档解密流程

```
用户打开加密文档
        │
        ▼
WpsMonitor检测到"文档已加密"对话框
        │
        ▼
获取文档路径（通过窗口标题匹配可能路径）
        │
        ▼
等待FileMeta初始化完成（最多5秒）
        │
        ▼
从FileMetaFactory获取文件元数据
        │
        ▼
判断是否已有自动填充尝试记录
        │
        ├─ 已尝试 → 跳过自动填充
        │
        ▼（未尝试）
标记自动填充尝试记录
        │
        ▼
从FileMeta获取CurrentPassword
        │
        ▼
PasswordAutoFiller填充密码并点击确定
        │
        ▼
等待对话框关闭（最多2.5秒）
        │
        ├─ 关闭成功 → 重置自动填充尝试记录
        └─ 关闭失败 → 保留尝试记录（密码可能错误）
```

### 5.2 文档加密流程

```
用户触发文档加密
        │
        ▼
WpsMonitor检测到"密码加密"对话框
        │
        ▼
获取文档路径和元数据
        │
        ▼
显示悬浮按钮（生成密码、提取密码、文档权限）
        │
        ▼（用户点击生成密码）
PasswordGenerator生成强密码
        │
        ▼
PasswordAutoFiller填充密码到加密对话框
        │
        ▼
通过UI Automation获取用户输入的密码
        │
        ▼（用户关闭加密对话框）
将密码保存到FileMeta的PendingPasswordList
        │
        ▼
文档关闭后，验证待定密码并写入元数据
        │
        ▼
PasswordReportService上报密码使用记录
```

### 5.3 元数据写入流程

```
文档关闭监控线程检测到文档关闭
        │
        ▼
检查文件是否被修改（修改时间 + 哈希值）
        │
        ▼
检查文档尾部是否存在UID元数据
        │
        ▼
从FileMetaFactory获取文件元数据
        │
        ▼
检查是否正在写入（IsWriting标记）
        │
        ▼
获取写入密码（优先验证待定密码）
        │
        ▼
FileMetaManager写入密码、UID、密钥版本到文件尾部
        │
        ▼
更新FileMeta的CurrentPassword
        │
        ▼
清空PendingPasswordList
        │
        ▼
PasswordReportService上报密码保存记录
```

### 5.4 用户登录流程

```
程序启动
        │
        ▼
检查本地存储的登录状态
        │
        ├─ 已登录 → 执行心跳检测
        │           │
        │           ├─ 心跳成功 → 更新UI状态
        │           └─ 心跳失败 → 显示登录窗口
        │
        ▼（未登录）
显示登录窗口
        │
        ▼
用户输入登录信息（用户名、密码、域名、端口）
        │
        ▼
验证输入格式（域名必须为HTTPS）
        │
        ▼
调用 /account/login 接口
        │
        ▼
保存用户信息和配置到本地存储
        │
        ▼
调用 /config/latest-key 获取公钥和密钥版本
        │
        ▼
启动监控线程
        │
        ▼
更新托盘菜单状态
```

## 6. 代码逻辑说明

### 6.1 监控线程主循环

**位置**：`Program.cs` 第554-889行

**核心逻辑**：

```
每500ms循环一次：
    │
    ├─ 检查登录状态（未登录则暂停，每秒检查）
    │
    ├─ 检查WPS是否运行
    │   │
    │   ├─ WPS运行中：
    │   │   │
    │   │   ├─ 检查权限窗口是否打开（打开则跳过）
    │   │   │
    │   │   ├─ 获取文档路径并初始化元数据
    │   │   │
    │   │   ├─ 检查文件格式是否支持
    │   │   │
    │   │   ├─ 检测加密对话框：
    │   │   │   │
    │   │   │   ├─ "密码加密" → 显示悬浮按钮，获取密码
    │   │   │   └─ "文档已加密" → 隐藏悬浮按钮
    │   │   │
    │   │   ├─ 检测解密对话框：
    │   │   │   │
    │   │   │   └─ "文档已加密" → 自动填充密码
    │   │   │
    │   │   └─ 无对话框 → 隐藏悬浮按钮，检查加密窗口是否刚关闭
    │   │
    │   └─ WPS未运行：隐藏悬浮按钮，重置记录
    │
    └─ 异常处理：记录日志，继续循环
```

### 6.2 文档关闭监控线程

**位置**：`Program.cs` 第900行开始

**核心逻辑**：

```
持续循环：
    │
    ├─ 获取所有已注册的文件路径
    │
    ├─ 遍历每个文件：
    │   │
    │   ├─ 判断文件是否已关闭
    │   │
    │   ├─ 已关闭则清理自动填充尝试记录
    │   │
    │   ├─ 获取文件元数据
    │   │
    │   ├─ 检查是否正在写入（IsWriting）
    │   │
    │   ├─ 判断是否需要写入元数据：
    │   │   │
    │   │   ├─ 文件内容已修改
    │   │   └─ 文件尾部无UID元数据
    │   │
    │   ├─ 获取写入密码（验证待定密码）
    │   │
    │   ├─ 设置IsWriting标记
    │   │
    │   ├─ 写入元数据（密码、UID、密钥版本）
    │   │
    │   ├─ 更新当前密码，清空待定密码
    │   │
    │   ├─ 上报密码保存记录
    │   │
    │   └─ 清除IsWriting标记
    │
    └─ 暂停一段时间后继续循环
```

### 6.3 密码填充逻辑

**位置**：`PasswordAutoFiller.cs` 第60-392行

**核心逻辑**：

```
1. 通过反射加载UI Automation程序集
2. 获取对话框的AutomationElement
3. 查找所有Edit控件
4. 根据控件名称匹配密码输入框：
   ├─ 加密对话框："打开文件密码"、"再次输入密码"
   └─ 解密对话框："请输入密码"
5. 获取ValuePattern并设置密码
6. 查找并点击确定按钮（"确定"、"OK"、"打开"）
```

### 6.4 元数据读写逻辑

**位置**：`FileMetaManager.cs`

**写入流程**：

```
1. 验证文件存在且格式支持
2. 加密密码（ECC+AES）
3. 构建元数据块（Magic + Version + Type + Length + Data + Checksum）
4. 查找ZIP文件的EOCD位置
5. 检查是否存在旧元数据并移除
6. 将新元数据写入文件末尾
7. 刷新文件流
```

**读取流程**：

```
1. 验证文件存在且格式支持
2. 读取文件尾部1KB数据
3. 从后向前搜索Magic标记
4. 解析元数据块（验证Version、Checksum）
5. 返回元数据类型和内容
```

## 7. 开发注意事项

### 7.1 线程安全

- **全局状态访问**：`GlobalState` 的所有属性访问都使用 `lock` 保护
- **元数据操作**：`FileMetaFactory` 使用 `ConcurrentDictionary` 和 `lock` 确保线程安全
- **文件操作**：使用 `FileShare.ReadWrite` 模式打开文件，避免文件锁定
- **日志写入**：使用异步队列和后台线程写入日志，避免阻塞主线程

### 7.2 异常处理

- **对话框识别失败**：不干预，放行手动操作，仅记录日志
- **元数据读取失败**：显示3秒提示框（"请手动输入密码"）
- **文档被占用**：重试3次，每次间隔500ms
- **网络请求失败**：自动重试3次，失败后提示用户
- **权限不足**：记录日志，跳过操作，无用户提示

### 7.3 DPI适配

- 使用 `DpiHelper` 获取窗口所在显示器的DPI缩放比例
- 所有UI控件尺寸和位置都需要乘以DPI缩放因子
- 悬浮按钮位置计算需考虑对话框所在显示器的DPI
- 使用 `AutoScaleMode.Dpi` 确保Windows Forms控件自动缩放

### 7.4 内存管理

- **文件元数据缓存**：`FileMetaFactory` 使用 `ConcurrentDictionary` 缓存元数据，文档关闭时清理
- **等待事件清理**：`AutoResetEvent` 在使用完毕后及时释放
- **资源释放**：所有 `IDisposable` 对象（文件流、加密对象等）都使用 `using` 语句
- **单例生命周期**：单例对象在程序退出时清理

### 7.5 安全注意

- **Token存储**：使用AES加密后存储到本地文件
- **密码传输**：使用ECC+AES混合加密后传输到服务器
- **公钥管理**：从服务器获取公钥，支持密钥版本更新
- **权限控制**：基于角色的访问控制（管理员可查看元数据）
- **权限校验**：操作前检查用户读写权限

### 7.6 性能优化

- **轮询间隔**：500ms扫描一次WPS窗口，平衡响应速度和CPU消耗
- **路径缓存**：文档路径匹配结果缓存，300秒清理一次
- **批量日志写入**：日志累积到8KB后批量写入文件
- **文件特征检测**：先检查修改时间，再计算哈希值，减少不必要的计算

### 7.7 版本兼容

- **WPS版本兼容**：支持WPS Office 2019/2021/12.x 及以上版本
- **操作系统适配**：支持Windows 10/11 64位系统
- **UI Automation版本**：支持不同版本的UIAutomationClient程序集

### 7.8 代码规范

- **命名规范**：PascalCase（类/方法）、camelCase（变量/参数）
- **文件编码**：UTF-8 with BOM
- **空值检查**：启用Nullable引用类型，所有引用类型需要空值检查
- **日志规范**：使用分级日志，关键操作记录详细日志，调试信息使用Debug级别

