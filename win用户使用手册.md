# WPS密码管理插件 - 用户使用手册

## 目录

1. [简介](#1-简介)
2. [系统要求](#2-系统要求)
3. [启动程序](#3-启动程序)
4. [登录配置](#4-登录配置)
5. [核心功能操作](#5-核心功能操作)
6. [常见问题解答](#6-常见问题解答)
7. [使用注意事项](#7-使用注意事项)

***

## 1. 简介

WPS密码管理插件是一款企业文档密码自动化管理工具，简化WPS文档的加密和解密流程，提升办公效率。

**主要功能**：

- **自动解密**：打开加密文档时自动填充密码，无元数据时自动弹出密码输入框
- **一键加密**：生成强密码并自动填充
- **密码存储**：密码安全存储在文档内部
- **权限管理**：配置文档访问权限

***

## 2. 系统要求

### 2.1 硬件要求

| 项目   | 最低配置            |
| ---- | --------------- |
| 操作系统 | Windows 10 64位  |
| CPU  | Intel i3 或等效处理器 |
| 内存   | 4GB             |

### 2.2 软件要求

| 软件           | 版本要求             |
| ------------ | ---------------- |
| WPS Office   | 12.1.0.26895 及以上 |
| .NET Runtime | 6.0 或以上          |

***

## 3. 启动程序

本软件为绿色软件，无需安装，直接运行即可：

1. 解压安装包到任意目录
2. 双击运行 `PasswordManager.exe`
3. 系统会提示用户账户控制权限请求，请点击"是"允许运行
4. 程序启动后，系统托盘会显示图标

**退出程序**：右键点击托盘图标，选择"退出"

***

## 4. 登录配置

### 4.1 登录界面

首次启动或未登录时，会显示登录窗口：

| 字段  | 说明                 | 必填 |
| --- | ------------------ | -- |
| 用户名 | 域账号                | 是  |
| 密码  | 域密码                | 是  |
| 域名  | 企业服务器地址            | 是  |
| 端口  | 服务器端口（必须填写，默认8443） | 是  |

### 4.2 登录步骤

1. 打开登录窗口（程序启动时自动显示，或双击托盘图标）
2. 填写登录信息，点击"登录"按钮
3. 登录成功后，登录窗口自动关闭
4. 如需注销，打开登录窗口点击"登录"按钮（已登录时按钮显示为注销）

**注意**：

- 域名必须以 `https://` 开头，不支持 `http://` 协议，没有则默认为`https://`
- 端口号为必填项，需输入有效的数字

***

## 5. 核心功能操作

### 5.1 文档自动解密

**使用场景**：打开已加密的WPS文档

**操作步骤**：

1. 确保插件已登录
2. 使用WPS打开加密文档（.docx、.xlsx、.pptx）
3. WPS弹出"文档已加密"密码输入框
4. 插件自动检测文档元数据：
   - 如果文档已有元数据（包含密码），插件自动识别并填充密码，文档自动打开
   - 如果文档无元数据（首次打开的加密文档），插件弹出密码输入框，请输入密码并点击"确认"
   - 如果输入的密码不正确，插件会再次弹出密码输入框，允许重新输入

**注意**：

- 插件启动后新建的加密文件，插件会自动识别密码并保存
- 插件启动前的历史加密文件（无元数据），首次打开时插件会弹出密码输入框，输入正确密码后插件会自动保存密码到文档元数据，下次打开时即可自动填充

### 5.2 文档一键加密

**使用场景**：对WPS文档进行密码加密

**操作步骤**：

1. 确保插件已登录
2. 在WPS中打开需要加密的文档
3. 通过WPS菜单触发加密功能（"文件" → "文档加密" → "密码加密"）
4. WPS弹出"密码加密"对话框，右侧显示悬浮按钮组
5. 点击"生成密码"按钮，插件自动生成并填充密码（也可以自行输入密码）
6. 点击WPS加密对话框的"确定"完成加密（**请确保输入框中的密码为明文状态下再关闭弹窗，否则可能导致插件识别不到密码**）

**悬浮按钮说明**：

| 按钮   | 功能            | 显示条件          |
| ---- | ------------- | ------------- |
| 生成密码 | 自动生成强密码并填充    | 始终显示          |
| 提取密码 | 获取当前密码并复制到剪贴板 | 始终显示          |
| 文档权限 | 配置文档访问权限      | 仅当用户为文档所属人时显示 |

### 5.3 文档权限配置

**使用场景**：配置哪些用户可以访问文档

**操作步骤**：

1. 在"密码加密"对话框中点击"文档权限"按钮（仅当用户为文档所属人时显示）
2. 权限配置窗口打开，显示部门和员工树
3. 展开部门节点，选择需要授权的员工
4. 点击"保存"按钮完成权限配置

***

## 6. 常见问题解答

### 6.1 登录问题

**Q1：登录时提示"网络连接失败"怎么办？**

**A**：请检查：

1. 网络连接正常，能访问企业服务器
2. 域名地址正确，且以 `https://` 开头
3. 端口号正确（默认8443）
4. 防火墙没有阻止程序访问网络

**Q2：忘记密码怎么办？**

**A**：联系企业IT部门重置密码。

### 6.2 文档操作问题

**Q3：打开加密文档时没有自动填充密码？**

**A**：插件会根据文档情况自动处理：

- 如果文档已有元数据（包含密码），插件会自动填充密码
- 如果文档无元数据（首次打开的加密文档），插件会弹出密码输入框，请输入密码并点击"确认"
- 如果输入的密码不正确，插件会再次弹出密码输入框，允许重新输入

请检查：

1. 插件已登录
2. 文档格式支持（.docx、.xlsx、.pptx）
3. 对话框标题为"文档已加密"

**Q4：加密文档时没有显示悬浮按钮？**

**A**：请检查：

1. 插件已登录
2. 对话框标题为"密码加密"
3. WPS版本支持（12.1.0.26895及以上）

### 6.3 兼容性问题

**Q5：支持哪些文档格式？**

**A**：支持 `.docx`、`.xlsx`、`.pptx` 格式，不支持旧版 `.doc`、`.xls`、`.ppt` 格式。

***

## 7. 使用注意事项

1. **登录后使用**：所有自动填充功能需要登录后才能使用
2. **首次密码输入**：首次打开无元数据的加密文档时，插件会自动弹出密码输入框，输入正确密码后自动保存
3. **密码变更处理**：如果文档密码变更，插件会弹出密码输入框，输入新密码后自动更新
4. **文档关闭时机**：加密文档后，请确保文档完全关闭，插件才能保存密码
5. **文档打开时机**：对于上一次关闭的文档，建议等待1\~2秒左右再次打开，防止文件元数据未写入完整

***

