# 密码管理插件 - 通用技术方案文�?

## 1. 文档概述

### 1.1 文档目的

本文档旨在详细阐述密码管理插件的技术架构、核心实现方案、数据流程、接口规范及扩展性设计，为类似办公软件插件开发提供标准化技术参考。文档内容具备通用性和规范性，可作为跨平台办公软件密码管理插件开发的技术指南�?

### 1.2 适用范围

- 密码管理插件开�?
- 跨平�?Office 文档密码管理方案设计
- 桌面应用程序 UI 自动化技术实�?
- 企业级文档安全管理系统集�?

### 1.3 技术基�?

| 项目 | 技术标�?|
|------|----------|
| 操作系统 | Windows 10/11 (64�? |
| 办公软件 | WPS Office 2019/2021/12.1.0.25225 |
| 开发框�?| .NET 6.0 Windows Desktop |
| 编程语言 | C# 10 |
| UI 框架 | Windows Forms |

## 2. 系统架构设计

### 2.1 整体架构

本插件采�?*分层架构模式**，将系统划分为表示层、业务逻辑层、服务层和数据层四个主要层次，各层职责明确，依赖关系清晰�?

```
┌─────────────────────────────────────────────────────────────�?
�?                   Presentation Layer (UI)                   �?
�? ┌─────────────�? ┌─────────────�? ┌─────────────────────�? �?
�? �? TrayIcon   �? �? LoginForm  �? �? FloatingButton     �? �?
�? └─────────────�? └─────────────�? └─────────────────────�? �?
└─────────────────────────────────────────────────────────────�?
                              �?
                              �?
┌─────────────────────────────────────────────────────────────�?
�?                   Business Logic Layer                      �?
�? ┌─────────────�? ┌─────────────�? ┌─────────────────────�? �?
�? �?FileMeta    �? �?OfficeEncrypt�?�?PasswordGenerator   �? �?
�? �?Manager     �? �?Utils       �? �?                    �? �?
�? └─────────────�? └─────────────�? └─────────────────────�? �?
�? ┌─────────────�? ┌─────────────�? ┌─────────────────────�? �?
�? �?AutoFill    �? �?WpsMonitor  �? �?FileMonitor        �? �?
�? �?Manager     �? �?            �? �?                    �? �?
�? └─────────────�? └─────────────�? └─────────────────────�? �?
└─────────────────────────────────────────────────────────────�?
                              �?
                              �?
┌─────────────────────────────────────────────────────────────�?
�?                      Service Layer                          �?
�? ┌─────────────�? ┌─────────────�? ┌─────────────────────�? �?
�? �?HttpRequest �? �?  ApiRoutes �? �?PasswordReport      �? �?
�? �?Service     �? �?            �? �?Service             �? �?
�? └─────────────�? └─────────────�? └─────────────────────�? �?
└─────────────────────────────────────────────────────────────�?
                              �?
                              �?
┌─────────────────────────────────────────────────────────────�?
�?                   Utility Layer (Utils)                    �?
�? ┌─────────────�? ┌─────────────�? ┌─────────────────────�? �?
�? �?GlobalState �? �?Storage     �? �?Logger              �? �?
�? �?            �? �?Manager     �? �?                    �? �?
�? └─────────────�? └─────────────�? └─────────────────────�? �?
�? ┌─────────────�?                                             �?
�? �?CryptoUtils �?                                             �?
�? └─────────────�?                                             �?
└─────────────────────────────────────────────────────────────�?
```

### 2.2 模块职责划分

#### 2.2.1 表示层（UI Layer�?

**职责**：负责用户交互界面的展示和用户输入的收集�?

**核心组件**�?

| 组件名称 | 类型 | 职责描述 |
|---------|------|----------|
| TrayIcon | 系统托盘 | 系统托盘图标管理、右键菜单、托盘通知 |
| LoginForm | 登录窗口 | 用户认证界面、登�?登出逻辑 |
| FloatingButton | 悬浮按钮 | 密码框旁的生成按钮、位置跟�?|
| NotificationForm | 通知表单 | 轻量提示框、自动关闭机�?|
| AuthTreeForm | 权限树表�?| 文档权限树展示和编辑 |

**设计原则**�?
- 最小化原则：无主界面，仅通过托盘图标提供核心交互
- 非阻塞原则：所�?UI 操作不阻塞后台监控线�?
- 响应式设计：支持 DPI 缩放、多显示器适配

#### 2.2.2 业务逻辑层（Business Layer�?

**职责**：实现核心业务逻辑，包括窗口监控、密码生成、文档元数据管理等�?

**核心组件**�?

| 组件名称 | 类型 | 职责描述 |
|---------|------|----------|
| WpsMonitor | WPS监控�?| WPS进程监控、密码对话框识别、窗口定�?|
| FileMonitor | 文件监控�?| 文件系统变化监控、触发业务逻辑 |
| PasswordAutoFiller | 密码填充�?| UI自动化密码填充、控件定�?|
| QtWindowLocator | Qt窗口定位�?| Qt框架窗口的子控件定位 |
| FileMetaManager | 元数据管理器 | 文件元数据的读写、缓存管�?|
| OfficeEncryptUtils | 加密工具 | Office文档加密状态检测、密码验�?|
| PasswordGenerator | 密码生成�?| 符合安全规范的强密码生成 |
| AutoFillAttemptManager | 填充管理�?| 自动填充尝试记录、失败重�?|

**设计原则**�?
- 单一职责：每个组件专注于单一业务领域
- 线程安全：多线程环境下的安全访问控制
- 异常隔离：单点故障不影响整体系统稳定�?

#### 2.2.3 服务层（Service Layer�?

**职责**：封�?HTTP 通信、API路由、报告生成等远程服务能力�?

**核心组件**�?

| 组件名称 | 类型 | 职责描述 |
|---------|------|----------|
| HttpRequestService | HTTP服务 | 封装GET/POST/PUT请求、超时控�?|
| RequestFactory | 请求工厂 | HTTP请求的构建和发�?|
| ApiRoutes | 路由常量 | 后端API路由定义 |
| ApiResponse | 响应模型 | 统一API响应格式 |
| PasswordReportService | 报告服务 | 密码使用情况报告生成 |

**设计原则**�?
- 接口抽象：统一的请�?响应模型
- 重试机制：网络异常自动重�?
- 超时控制：防止请求无限等�?

#### 2.2.4 工具层（Utility Layer�?

**职责**：提供全局状态管理、本地存储、日志记录等通用能力�?

**核心组件**�?

| 组件名称 | 类型 | 职责描述 |
|---------|------|----------|
| GlobalState | 全局状�?| 单例模式的用户信息、配置、登录状�?|
| StorageManager | 存储管理 | 本地配置、用户信息加密存�?|
| Logger | 日志记录 | 分级日志、文件输�?|
| CryptoUtils | 加密工具 | AES加密解密、密码学操作 |

### 2.3 层次依赖关系

```
┌────────────────────────────────────────────�?
�?          Presentation Layer              �?
�?   (UI, Forms, Controls, TrayIcon)        �?
└─────────────────┬──────────────────────────�?
                  �?依赖
                  �?
┌────────────────────────────────────────────�?
�?         Business Logic Layer              �?
�?   (Monitors, Managers, Utilities)         �?
└─────────────────┬──────────────────────────�?
                  �?依赖
                  �?
┌────────────────────────────────────────────�?
�?            Service Layer                  �?
�?     (HTTP, API, Report Services)          �?
└─────────────────┬──────────────────────────�?
                  �?依赖
                  �?
┌────────────────────────────────────────────�?
�?           Utility Layer                   �?
�?     (GlobalState, Logger, Storage)        �?
└────────────────────────────────────────────�?
```

**依赖规则**�?
- 上层可依赖下层，下层不可依赖上层
- 同层之间通过接口抽象进行松耦合
- 禁止跨层直接依赖

## 3. 核心技术实现方�?

### 3.1 UI 自动化技术方�?

#### 3.1.1 窗口监控机制

插件通过 Win32 API 实现 WPS 窗口的实时监控，核心实现如下�?

```csharp
// 窗口枚举回调
private delegate bool EnumWindowsProc(IntPtr hWnd, IntPtr lParam);

[DllImport("user32.dll")]
private static extern bool EnumWindows(EnumWindowsProc lpEnumFunc, IntPtr lParam);

[DllImport("user32.dll")]
private static extern bool IsWindowVisible(IntPtr hWnd);

[DllImport("user32.dll")]
private static extern int GetWindowText(IntPtr hWnd, StringBuilder lpString, int nMaxCount);

[DllImport("user32.dll")]
private static extern uint GetWindowThreadProcessId(IntPtr hWnd, out uint lpdwProcessId);
```

**监控策略**�?
1. 定时轮询：每 100ms 枚举一�?WPS 窗口
2. 事件驱动：监听窗口创�?销毁消�?
3. 进程关联：通过进程 ID 过滤 WPS 相关窗口

#### 3.1.2 Qt 窗口控件定位

WPS Office 使用 Qt 框架开发，传统�?`FindWindowEx` 无法直接定位子控件。本插件采用以下方案�?

```csharp
// Qt窗口定位核心逻辑
public class QtWindowLocator
{
    // Qt编辑控件识别
    private IntPtr FindQtEditControl(IntPtr parentWindow)
    {
        // 1. 枚举所有子窗口
        // 2. 获取窗口类名
        // 3. 匹配Qt控件特征 (Qt5QWindowIcon, QWidget�?
        // 4. 通过窗口标题精确匹配
    }
    
    // 递归查找子控�?
    private IntPtr FindChildControl(IntPtr parent, string controlName)
    {
        // 深度优先遍历窗口�?
        // 匹配控件名称和类�?
    }
}
```

**定位算法**�?
1. **窗口树遍�?*：递归枚举目标窗口的所有子窗口
2. **类名匹配**：识�?Qt 控件特征类名
3. **标题匹配**：通过控件标题精确定位
4. **多策略融�?*：尝试多种匹配策略组合定�?

#### 3.1.3 UI Automation 集成

�?Win32 API 无法满足需求时，插件使�?.NET UI Automation 技术：

```csharp
// UI Automation密码填充
public bool FillPasswordByUIAutomation(IntPtr dialogHandle, string password)
{
    // 1. 获取AutomationElement
    var dialogElement = AutomationElement.FromHandle(dialogHandle);
    
    // 2. 查找Edit控件
    var editCondition = new PropertyCondition(
        AutomationElement.ControlTypeProperty, 
        ControlType.Edit
    );
    var editElements = dialogElement.FindAll(TreeScope.Descendants, editCondition);
    
    // 3. 获取ValuePattern并设置密�?
    var valuePattern = (ValuePattern)editElement.GetCurrentPattern(ValuePattern.Pattern);
    valuePattern.SetValue(password);
    
    // 4. 点击确定按钮
    var okButton = FindOkButton(dialogElement);
    var invokePattern = (InvokePattern)okButton.GetCurrentPattern(InvokePattern.Pattern);
    invokePattern.Invoke();
}
```

### 3.2 Office 文档处理方案

#### 3.2.1 加密状态检�?

通过文件签名和结构分析判断文档加密状态：

```csharp
public static bool IsFileEncrypted(string filePath)
{
    // 1. OLE格式检�?(D0 CF 11 E0)
    if (HasOleSignature(filePath))
    {
        using var fs = new FileStream(filePath, FileMode.Open, FileAccess.Read);
        var npoiFs = new NPOIFSFileSystem(fs);
        try {
            new EncryptionInfo(npoiFs);
            return true;  // OLE加密文档
        } catch {
            return false;
        }
    }
    
    // 2. Open XML格式检�?(ZIP + EncryptedPackage)
    using var zip = new ZipArchive(fs, ZipArchiveMode.Read);
    return zip.Entries.Any(e => 
        e.FullName.Equals("EncryptedPackage", StringComparison.OrdinalIgnoreCase)
    );
}
```

**检测策�?*�?
- **OLE格式**�?doc/.xls/.ppt）：检测复合文档加密标�?
- **Open XML格式**�?docx/.xlsx/.pptx）：检�?EncryptedPackage 入口
- **文件签名**：通过魔数（Magic Number）识别文件类�?

#### 3.2.2 密码验证实现

使用 NPOI 库验�?Office 文档密码�?

```csharp
public bool VerifyPassword(string filePath, string password)
{
    using var fs = new FileStream(filePath, FileMode.Open, FileAccess.Read);
    
    // 检测文件格�?
    var header = new byte[8];
    fs.Read(header, 0, 8);
    bool isOleFormat = IsOleSignature(header);
    
    fs.Seek(0, SeekOrigin.Begin);
    
    if (isOleFormat)
    {
        // OLE格式密码验证
        var npoiFs = new NPOIFSFileSystem(fs);
        var encryptInfo = new EncryptionInfo(npoiFs);
        var decryptor = Decryptor.GetInstance(encryptInfo);
        return decryptor.VerifyPassword(password);
    }
    else
    {
        // Open XML格式处理
        // ...
    }
}
```

### 3.3 文档元数据管理方�?

#### 3.3.1 元数据存储结�?

密码信息存储�?Office 文档的自定义元数据中�?

```csharp
public class FileMeta
{
    public string FilePath { get; set; }           // 文件绝对路径
    public string Uid { get; set; }                // 文件权限标识
    public string CurrentPassword { get; set; }     // 已确认的密码
    public string AfterPassword { get; set; }       // 待写入的新密�?
    public SortedSet<string> PendingPasswordList { get; set; }  // 待定密码集合
    public string OwnerAccount { get; set; }        // 文档所属账�?
    public string OwnerName { get; set; }          // 文档所属名�?
    public bool ReadAuth { get; set; }             // 读权�?
    public bool WriteAuth { get; set; }            // 写权�?
    public string CurrentKeyVersion { get; set; }   // 密钥版本
}
```

**存储位置**：Office 文档 �?文件属�?�?自定义属�?�?隐藏字段

#### 3.3.2 元数据读写机�?

```csharp
public class FileMetaManager
{
    public void SaveFileMeta(FileMeta meta)
    {
        // 1. 打开文档
        using var doc = WordprocessingDocument.Open(meta.FilePath, true);
        
        // 2. 获取扩展属�?
        var extendedPart = doc.ExtendedFilePropertiesPart;
        var properties = extendedPart.Properties;
        
        // 3. 写入自定义元数据
        // Properties["CustomProperty"] = encryptedPassword
        
        // 4. 保存
        properties.Save();
    }
}
```

### 3.4 强密码生成方�?

#### 3.4.1 密码规范

生成的密码必须满足以下安全规范：

| 字符类型 | 要求 | 说明 |
|---------|------|------|
| 大写字母 | 至少2�?| A-Z |
| 小写字母 | 至少2�?| a-z |
| 数字 | 至少2�?| 0-9 |
| 特殊符号 | 至少2�?| !@#$%^&* |
| 总长�?| 16�?| - |
| 连续字符 | 不允�?| �?abc, 123�?|
| 重复字符 | 不允�?| �?aa, 11�?|

#### 3.4.2 生成算法

```csharp
public class PasswordGenerator
{
    private const string UPPERCASE = "ABCDEFGHJKLMNPQRSTUVWXYZ";  // 排除易混淆字�?
    private const string LOWERCASE = "abcdefghjkmnpqrstuvwxyz";
    private const string DIGITS = "23456789";  // 排除0�?
    private const string SPECIAL = "!@#$%^&*";
    
    public string GenerateStrongPassword(int length = 16)
    {
        // 1. 确保每种字符类型至少2�?
        var password = new char[length];
        password[0] = GetRandomChar(UPPERCASE);
        password[1] = GetRandomChar(UPPERCASE);
        password[2] = GetRandomChar(LOWERCASE);
        password[3] = GetRandomChar(LOWERCASE);
        password[4] = GetRandomChar(DIGITS);
        password[5] = GetRandomChar(DIGITS);
        password[6] = GetRandomChar(SPECIAL);
        password[7] = GetRandomChar(SPECIAL);
        
        // 2. 填充剩余位置
        var allChars = UPPERCASE + LOWERCASE + DIGITS + SPECIAL;
        for (int i = 8; i < length; i++)
        {
            password[i] = GetRandomChar(allChars);
        }
        
        // 3. Fisher-Yates洗牌
        Shuffle(password);
        
        return new string(password);
    }
}
```

## 4. 数据流程设计

### 4.1 文档加密流程

```
┌─────────────�?    ┌─────────────�?    ┌─────────────�?    ┌─────────────�?
�?用户触发    �?──�?�?WpsMonitor  �?──�?�?窗口定位    �?──�?�?悬浮按钮    �?
�?文档加密    �?    �?检测对话框  �?    �?Qt控件定位  �?    �?显示按钮    �?
└─────────────�?    └─────────────�?    └─────────────�?    └─────────────�?
                                                                �?
                                                                �?
┌─────────────�?    ┌─────────────�?    ┌─────────────�?    ┌─────────────�?
�?文档保存    �?◀── �?元数据写�? �?◀── �?密码生成    �?◀── �?用户点击    �?
�?加密完成    �?    �?存储密码    �?    �?强密码算�? �?    �?生成按钮    �?
└─────────────�?    └─────────────�?    └─────────────�?    └─────────────�?
```

**流程说明**�?

1. **对话框检�?*：WpsMonitor �?100ms 轮询 WPS 窗口，识别「文档加密」对话框
2. **控件定位**：QtWindowLocator 通过窗口树遍历定位密码输入框
3. **按钮显示**：FloatingButton 在输入框右侧 5px 位置显示
4. **密码生成**：PasswordGenerator 生成 16 位符合规范的强密�?
5. **自动填充**：PasswordAutoFiller 通过 UI Automation 填充密码
6. **元数据写�?*：FileMetaManager 在文档保存后写入密码元数�?

### 4.2 文档解密流程

```
┌─────────────�?    ┌─────────────�?    ┌─────────────�?    ┌─────────────�?
�?用户打开    �?──�?�?WpsMonitor  �?──�?�?文档路径    �?──�?�?元数据读�? �?
�?加密文档    �?    �?检测解密框  �?    �?自动解析    �?    �?获取密码    �?
└─────────────�?    └─────────────�?    └─────────────�?    └─────────────�?
                                                                �?
                                                                �?
┌─────────────�?    ┌─────────────�?    ┌─────────────�?    ┌─────────────�?
�?文档打开    �?◀── �?密码验证    �?◀── �?自动填充    �?◀── �?密码定位    �?
�?解密成功    �?    �?成功确认    �?    �?UI自动�?   �?    �?控件定位    �?
└─────────────�?    └─────────────�?    └─────────────�?    └─────────────�?
```

**流程说明**�?

1. **解密检�?*：WpsMonitor 识别「密码输入」对话框
2. **路径解析**：通过窗口标题�?Recent 文件夹解析文档路�?
3. **元数据读�?*：FileMetaManager 读取文档自定义属性中的密�?
4. **密码定位**：QtWindowLocator 定位密码输入�?
5. **自动填充**：PasswordAutoFiller 填充密码并点击确�?
6. **验证确认**：OfficeEncryptUtils 验证密码正确�?

### 4.3 用户认证流程

```
┌─────────────�?    ┌─────────────�?    ┌─────────────�?    ┌─────────────�?
�?用户输入    �?──�?�?表单验证    �?──�?�?HTTP请求    �?──�?�?Token存储   �?
�?登录信息    �?    �?格式校验    �?    �?调用后端API �?    �?全局状�?   �?
└─────────────�?    └─────────────�?    └─────────────�?    └─────────────�?
                                                                �?
                                                                �?
┌─────────────�?    ┌─────────────�?    ┌─────────────�?    ┌─────────────�?
�?监控启动    �?◀── �?状态更�?   �?◀── �?响应解析    �?◀── �?服务器验�? �?
�?功能启用    �?    �?UI更新      �?    �?获取Token  �?    �?返回用户信息�?
└─────────────�?    └─────────────�?    └─────────────�?    └─────────────�?
```

### 4.4 异常处理流程

```
┌─────────────�?    ┌─────────────�?    ┌─────────────�?    ┌─────────────�?
�?业务操作    �?──�?�?异常捕获    �?──�?�?异常分类    �?──�?�?分类处理    �?
�?执行        �?    �?try-catch   �?    �?类型判定    �?    �?策略选择    �?
└─────────────�?    └─────────────�?    └─────────────�?    └─────────────�?
                           �?
           ┌───────────────┼───────────────�?
           �?              �?              �?
    ┌─────────────�?┌─────────────�?┌─────────────�?
    �?可恢复异�? �?�?不可恢复异常�?�?用户误操�? �?
    �?重试机制   �?�?日志记录    �?�?轻量提示    �?
    └─────────────�?└─────────────�?└─────────────�?
```

**异常分类处理策略**�?

| 异常类型 | 处理策略 | 用户感知 |
|---------|---------|---------|
| 对话框识别失�?| 不干预，放行手动操作 | 无提�?|
| 元数据读取失�?| 显示3秒提示框 | 「请手动输入密码�?|
| 文档被占�?| 重试3次，每次间隔500ms | 重试后提�?|
| 网络请求失败 | 自动重试3�?| 失败后提�?|
| 权限不足 | 记录日志，跳过操�?| 无提�?|

## 5. 接口规范设计

### 5.1 RESTful API 接口

#### 5.1.1 认证接口

**POST /account/login** - 用户登录

```json
// Request
{
    "account": "string",
    "password": "string"
}

// Response (200 OK)
{
    "status": 200,
    "message": "登录成功",
    "date": "2024-01-01 10:00:00",
    "data": {
        "token": "string",
        "account": "string",
        "name": "string"
    }
}
```

**POST /account/refresh-token** - 刷新令牌

```json
// Request (Header: token)
// Response (200 OK)
{
    "status": 200,
    "message": "刷新成功",
    "date": "2024-01-01 10:30:00",
    "data": {
        "token": "string",
        "account": "string",
        "name": "string"
    }
}
```

**POST /account/logout** - 用户登出

```json
// Request (Header: token)
// Response (200 OK)
{
    "status": 200,
    "message": "登出成功",
    "date": "2024-01-01 18:00:00",
    "data": null
}
```

#### 5.1.2 文档接口

**GET /doc/password** - 获取文档密码

```json
// Request (Header: token)
// Query Parameters: filePath
// Response (200 OK)
{
    "status": 200,
    "message": "获取成功",
    "date": "2024-01-01 10:05:00",
    "data": {
        "password": "string",
        "keyVersion": "string"
    }
}
```

**GET /doc/auth/tree** - 获取文档权限�?

```json
// Request (Header: token)
// Query Parameters: filePath
// Response (200 OK)
{
    "status": 200,
    "message": "获取成功",
    "date": "2024-01-01 10:10:00",
    "data": {
        "treeId": "string",
        "nodes": [
            {
                "id": "string",
                "name": "string",
                "type": "user|department|role",
                "readAuth": true,
                "writeAuth": true,
                "children": []
            }
        ]
    }
}
```

**PUT /doc/auth/update** - 更新文档权限

```json
// Request (Header: token)
{
    "filePath": "string",
    "uid": "string",
    "readAuth": true,
    "writeAuth": false
}

// Response (200 OK)
{
    "status": 200,
    "message": "更新成功",
    "date": "2024-01-01 10:15:00",
    "data": null
}
```

**POST /doc/save/log** - 上报保存记录

```json
// Request (Header: token)
{
    "filePath": "string",
    "uid": "string",
    "operation": "encrypt|decrypt|update",
    "timestamp": "2024-01-01 10:20:00"
}

// Response (200 OK)
{
    "status": 200,
    "message": "上报成功",
    "date": "2024-01-01 10:20:00",
    "data": null
}
```

#### 5.1.3 配置接口

**GET /config/latest-key** - 获取最新密�?

```json
// Request (Header: token)
// Response (200 OK)
{
    "status": 200,
    "message": "获取成功",
    "date": "2024-01-01 10:00:00",
    "data": {
        "publicKey": "string",
        "keyVersion": "string"
    }
}
```

**POST /config/encrypt** - 公钥加密

```json
// Request (Header: token)
{
    "data": "string"
}

// Response (200 OK)
{
    "status": 200,
    "message": "加密成功",
    "date": "2024-01-01 10:00:00",
    "data": {
        "encryptedData": "string"
    }
}
```

### 5.2 内部事件接口

插件内部通过事件机制实现模块间通信�?

```csharp
// 事件定义
public static class Events
{
    // 窗口事件
    public static event EventHandler<PasswordDialogEventArgs> PasswordDialogDetected;
    public static event EventHandler<DecryptDialogEventArgs> DecryptDialogDetected;
    
    // 文件事件
    public static event EventHandler<FileEncryptEventArgs> FileEncrypted;
    public static event EventHandler<FileDecryptEventArgs> FileDecrypted;
    
    // 认证事件
    public static event EventHandler<LoginEventArgs> UserLoggedIn;
    public static event EventHandler<LogoutEventArgs> UserLoggedOut;
}

// 事件参数
public class PasswordDialogEventArgs : EventArgs
{
    public IntPtr DialogHandle { get; set; }
    public string WindowTitle { get; set; }
    public DateTime DetectedTime { get; set; }
}
```

### 5.3 插件扩展接口

为支持插件扩展，预留以下扩展点：

```csharp
// 密码提供者接�?
public interface IPasswordProvider
{
    Task<string> GetPasswordAsync(string filePath);
    Task<bool> SetPasswordAsync(string filePath, string password);
    Task<bool> VerifyPasswordAsync(string filePath, string password);
}

// UI自动化提供者接�?
public interface IUIAutomationProvider
{
    bool FillPassword(IntPtr dialogHandle, string password);
    bool ClickButton(IntPtr dialogHandle, string buttonText);
    IntPtr FindControl(IntPtr parentHandle, string controlName);
}

// 通知接口
public interface INotificationService
{
    void ShowInfo(string message);
    void ShowWarning(string message);
    void ShowError(string message);
}
```

## 6. 扩展性设�?

### 6.1 插件化架�?

#### 6.1.1 模块插件�?

采用依赖注入（DI）实现模块解耦：

```csharp
public interface IServiceProvider
{
    T GetService<T>() where T : class;
    void Register<TInterface, TImplementation>() 
        where TInterface : class 
        where TImplementation : class, TInterface;
}

public class ServiceContainer : IServiceProvider
{
    private readonly Dictionary<Type, Type> _registrations = new();
    private readonly Dictionary<Type, object> _singletons = new();
    
    public void Register<TInterface, TImplementation>()
        where TInterface : class
        where TImplementation : class, TInterface
    {
        _registrations[typeof(TInterface)] = typeof(TImplementation);
    }
    
    public T GetService<T>() where T : class
    {
        var interfaceType = typeof(T);
        if (_singletons.ContainsKey(interfaceType))
        {
            return (T)_singletons[interfaceType];
        }
        
        if (_registrations.TryGetValue(interfaceType, out var implType))
        {
            var instance = Activator.CreateInstance(implType);
            _singletons[interfaceType] = instance;
            return (T)instance;
        }
        
        throw new InvalidOperationException($"Service {interfaceType.Name} not registered");
    }
}
```

#### 6.1.2 配置驱动

通过配置文件支持功能开关和参数调整�?

```json
{
    "plugin": {
        "name": "PasswordManager",
        "version": "1.0.0",
        "enabled": true
    },
    "monitor": {
        "pollInterval": 100,
        "enableProcessCheck": true,
        "enableWindowEnum": true
    },
    "automation": {
        "useUIAutomation": true,
        "useWin32API": true,
        "retryCount": 3,
        "retryDelay": 500
    },
    "password": {
        "length": 16,
        "includeUppercase": true,
        "includeLowercase": true,
        "includeDigits": true,
        "includeSpecial": true,
        "minCharTypeCount": 2
    },
    "storage": {
        "enableMetadata": true,
        "enableCache": true,
        "cacheExpiration": 3600
    },
    "network": {
        "timeout": 30000,
        "retryCount": 3,
        "enableHeartbeat": true,
        "heartbeatInterval": 60000
    },
    "ui": {
        "showFloatingButton": true,
        "buttonOffsetX": 5,
        "buttonHeight": 24,
        "showNotifications": true,
        "notificationDuration": 3000
    }
}
```

### 6.2 多版本兼容�?

#### 6.2.1 版本检测机�?

```csharp
public class VersionAdapter
{
    private readonly Version _currentVersion;
    private readonly Dictionary<string, IVersionHandler> _handlers;
    
    public VersionAdapter()
    {
        _currentVersion = GetWpsVersion();
        _handlers = new Dictionary<string, IVersionHandler>
        {
            { "12.1.0.25225", new Version121025225Handler() },
            { "2021", new Version2021Handler() },
            { "2019", new Version2019Handler() }
        };
    }
    
    public IVersionHandler GetHandler()
    {
        // 精确匹配优先
        if (_handlers.TryGetValue(_currentVersion.ToString(), out var handler))
            return handler;
        
        // 主版本号匹配
        var majorVersion = $"{_currentVersion.Major}.{_currentVersion.Minor}";
        foreach (var kvp in _handlers)
        {
            if (kvp.Key.StartsWith(majorVersion))
                return kvp.Value;
        }
        
        // 默认处理�?
        return new DefaultVersionHandler();
    }
}

public interface IVersionHandler
{
    IntPtr FindPasswordDialog();
    IntPtr FindEditControl(IntPtr parent);
    bool IsEncryptDialog(IntPtr hwnd);
    bool IsDecryptDialog(IntPtr hwnd);
}
```

#### 6.2.2 操作系统适配

```csharp
public static class OSAdapter
{
    public static bool IsWindows10OrAbove()
    {
        var version = Environment.OSVersion;
        return version.Platform == PlatformID.Win32NT 
            && version.Version.Major >= 10;
    }
    
    public static float GetSystemDpiScale()
    {
        if (IsWindows10OrAbove())
        {
            return GetWindows10DpiScale();
        }
        else
        {
            return 1.0f;  // Windows 7 默认100%
        }
    }
    
    private static float GetWindows10DpiScale()
    {
        var hWnd = Process.GetCurrentProcess().MainWindowHandle;
        var hMonitor = MonitorFromWindow(hWnd, 0);
        GetDpiForMonitor(hMonitor, MDT_EFFECTIVE_DPI, out uint dpiX, out _);
        return dpiX / 96.0f;
    }
}
```

### 6.3 功能扩展预留

#### 6.3.1 密码管理扩展

```csharp
// 密码策略接口
public interface IPasswordPolicy
{
    int MinLength { get; }
    int MaxLength { get; }
    bool RequireUppercase { get; }
    bool RequireLowercase { get; }
    bool RequireDigit { get; }
    bool RequireSpecialChar { get; }
    string AllowedSpecialChars { get; }
    string[] ForbiddenPatterns { get; }
}

// 内置策略
public class DefaultPasswordPolicy : IPasswordPolicy
{
    public int MinLength => 16;
    public int MaxLength => 32;
    public bool RequireUppercase => true;
    public bool RequireLowercase => true;
    public bool RequireDigit => true;
    public bool RequireSpecialChar => true;
    public string AllowedSpecialChars => "!@#$%^&*";
    public string[] ForbiddenPatterns => new[] { "abc", "123", "password" };
}
```

#### 6.3.2 报告扩展

```csharp
// 报告生成器接�?
public interface IReportGenerator
{
    Task<Report> GenerateAsync(DateTime startDate, DateTime endDate);
    Task ExportAsync(Report report, string format);
}

// 报告类型
public enum ReportType
{
    UsageSummary,      // 使用摘要
    PasswordAudit,    // 密码审计
    AccessLog,        // 访问日志
    SecurityAlert     // 安全告警
}
```

## 7. 外部系统集成方案

### 7.1 后端服务集成

#### 7.1.1 服务发现机制

```csharp
public class ServiceDiscovery
{
    private readonly HttpClient _httpClient;
    private readonly string _serviceEndpoint;
    
    public async Task<ServiceInfo> DiscoverAsync()
    {
        // 1. 尝试本地配置
        var localConfig = LoadLocalConfig();
        if (localConfig != null && await TestConnectionAsync(localConfig))
            return localConfig;
        
        // 2. 尝试服务注册中心
        var registryConfig = await QueryServiceRegistry();
        if (registryConfig != null)
            return registryConfig;
        
        // 3. 使用默认配置
        return GetDefaultConfig();
    }
}
```

#### 7.1.2 认证授权集成

```csharp
public class AuthIntegration
{
    private readonly ITokenManager _tokenManager;
    
    // LDAP认证集成
    public async Task<bool> LdapAuthAsync(string username, string password)
    {
        var ldapConfig = await _httpService.GetAsync<LdapConfig>(ApiRoutes.ConfigLdap);
        using var context = new LdapContext(ldapConfig.Server, ldapConfig.Port);
        return await Task.Run(() => context.ValidateCredentials(username, password));
    }
    
    // Token自动刷新
    public async Task EnsureValidTokenAsync()
    {
        if (_tokenManager.IsExpiringSoon())
        {
            await _tokenManager.RefreshAsync();
        }
    }
}
```

### 7.2 办公软件集成

#### 7.2.1 进程通信

```csharp
public class ProcessCommunication
{
    // 命名管道通信
    public void SendCommand(string command)
    {
        using var pipe = new NamedPipeClientStream(".", "WpsPasswordPlugin", PipeDirection.Out);
        pipe.Connect(5000);
        
        using var writer = new StreamWriter(pipe);
        writer.WriteLine(command);
        writer.Flush();
    }
}
```

#### 7.2.2 文件监控集成

```csharp
public class FileSystemIntegration
{
    private readonly FileSystemWatcher _watcher;
    
    public FileSystemIntegration(string watchPath)
    {
        _watcher = new FileSystemWatcher(watchPath)
        {
            Filter = "*.docx",
            NotifyFilter = NotifyFilters.LastWrite | NotifyFilters.FileName,
            IncludeSubdirectories = true
        };
        
        _watcher.Changed += OnFileChanged;
        _watcher.Created += OnFileCreated;
    }
    
    private void OnFileChanged(object sender, FileSystemEventArgs e)
    {
        // 触发元数据更新逻辑
        Events.FileModified?.Invoke(this, new FileModifiedEventArgs(e.FullPath));
    }
}
```

### 7.3 企业系统集成

#### 7.3.1 SSO 单点登录

```csharp
public class SSOIntegration
{
    // SAML 2.0 集成
    public async Task<SamlAssertion> GetAssertionAsync(string samlResponse)
    {
        var samlConfig = await _configService.GetAsync<SamlConfig>("saml");
        var parser = new SamlResponseParser(samlConfig.IdpCertificate);
        return parser.Parse(samlResponse);
    }
    
    // OAuth 2.0 集成
    public async Task<OAuthToken> ExchangeCodeAsync(string code)
    {
        var oauthConfig = await _configService.GetAsync<OAuthConfig>("oauth");
        var token = await _httpClient.PostAsync<OAuthToken>(
            oauthConfig.TokenEndpoint,
            new { code, grant_type = "authorization_code" }
        );
        return token;
    }
}
```

#### 7.3.2 审计日志集成

```csharp
public class AuditIntegration
{
    private readonly IAuditClient _auditClient;
    
    public async Task LogAsync(AuditEvent auditEvent)
    {
        var auditEntry = new AuditEntry
        {
            Timestamp = DateTime.UtcNow,
            UserId = GlobalState.Instance.Username,
            Action = auditEvent.Action,
            Resource = auditEvent.Resource,
            Result = auditEvent.Result,
            IpAddress = GetClientIP(),
            UserAgent = GetUserAgent()
        };
        
        await _auditClient.SubmitAsync(auditEntry);
    }
}
```

## 8. 安全设计

### 8.1 数据安全

#### 8.1.1 敏感数据加密

```csharp
public class SecureStorage
{
    private readonly byte[] _encryptionKey;
    
    public SecureStorage()
    {
        // �?DPAPI 获取用户级密�?
        var protectedKey = GetProtectedKeyFromDPAPI();
        _encryptionKey = ProtectedData.Unprotect(
            protectedKey, 
            null, 
            DataProtectionScope.CurrentUser
        );
    }
    
    public void SaveSecureData(string key, string value)
    {
        var encrypted = EncryptAes(value, _encryptionKey);
        // 保存到安全存�?
    }
}
```

#### 8.1.2 传输安全

- HTTPS 强制使用
- 证书校验
- 请求签名

```csharp
public class SecureHttpClient
{
    private readonly HttpClient _httpClient;
    
    public SecureHttpClient()
    {
        var handler = new HttpClientHandler
        {
            ServerCertificateCustomValidationCallback = ValidateCertificate
        };
        _httpClient = new HttpClient(handler);
    }
    
    private bool ValidateCertificate(
        HttpRequestMessage request,
        X509Certificate2 certificate,
        X509Chain chain,
        SslPolicyErrors errors)
    {
        // 严格的证书校验逻辑
        return errors == SslPolicyErrors.None;
    }
}
```

### 8.2 操作安全

#### 8.2.1 权限控制

```csharp
public class PermissionManager
{
    public bool HasPermission(string resource, PermissionType permission)
    {
        var userPermissions = GetUserPermissions();
        return userPermissions.Any(p => 
            p.Resource == resource && p.Type == permission);
    }
    
    public void RequirePermission(string resource, PermissionType permission)
    {
        if (!HasPermission(resource, permission))
        {
            throw new UnauthorizedAccessException(
                $"User lacks {permission} permission for {resource}");
        }
    }
}
```

#### 8.2.2 操作审计

所有敏感操作均记录审计日志�?

```csharp
[AuditLog]
public async Task UpdatePasswordAsync(string filePath, string newPassword)
{
    AuditContext.Start(filePath, "update_password");
    try
    {
        // 执行密码更新
        await _metaManager.UpdatePasswordAsync(filePath, newPassword);
        AuditContext.Success();
    }
    catch (Exception ex)
    {
        AuditContext.Failure(ex.Message);
        throw;
    }
}
```

## 9. 性能优化策略

### 9.1 资源优化

#### 9.1.1 内存管理

- 对象池复�?
- 弱引用缓�?
- 及时资源释放

```csharp
public class ObjectPool<T> where T : class
{
    private readonly ConcurrentBag<T> _objects;
    private readonly Func<T> _objectFactory;
    
    public ObjectPool(Func<T> objectFactory)
    {
        _objectFactory = objectFactory;
        _objects = new ConcurrentBag<T>();
    }
    
    public T Get()
    {
        if (_objects.TryTake(out var item))
            return item;
        return _objectFactory();
    }
    
    public void Return(T item)
    {
        _objects.Add(item);
    }
}
```

#### 9.1.2 CPU 优化

- 后台线程处理
- 定时器批量处�?
- 事件驱动替代轮询

```csharp
public class OptimizedMonitor
{
    private Timer _pollTimer;
    private readonly object _lock = new();
    private bool _isProcessing;
    
    public void Start()
    {
        _pollTimer = new Timer(ProcessCallback, null, 0, 100);
    }
    
    private void ProcessCallback(object state)
    {
        lock (_lock)
        {
            if (_isProcessing) return;
            _isProcessing = true;
        }
        
        try
        {
            // 批量处理
            var events = CollectEvents();
            ProcessBatch(events);
        }
        finally
        {
            _isProcessing = false;
        }
    }
}
```

### 9.2 启动优化

- 延迟初始化非核心组件
- 异步加载配置
- 后台服务预热

```csharp
public class LazyInitializer
{
    private static readonly Lazy<HeavyService> _heavyService = 
        new(() => new HeavyService(), LazyThreadSafetyMode.ExecutionAndPublication);
    
    public HeavyService HeavyService => _heavyService.Value;
}
```

## 10. 测试策略

### 10.1 单元测试

```csharp
[TestClass]
public class PasswordGeneratorTests
{
    [TestMethod]
    public void GeneratePassword_ShouldMeetRequirements()
    {
        var generator = new PasswordGenerator();
        var password = generator.GenerateStrongPassword(16);
        
        Assert.IsTrue(password.Length == 16);
        Assert.IsTrue(password.Any(char.IsUpper));
        Assert.IsTrue(password.Any(char.IsLower));
        Assert.IsTrue(password.Any(char.IsDigit));
        Assert.IsTrue(password.Any(c => "!@#$%^&*".Contains(c)));
    }
    
    [TestMethod]
    public void GeneratePassword_ShouldBeUnique()
    {
        var generator = new PasswordGenerator();
        var passwords = Enumerable.Range(0, 100)
            .Select(_ => generator.GenerateStrongPassword())
            .ToList();
        
        Assert.AreEqual(100, passwords.Distinct().Count());
    }
}
```

### 10.2 集成测试

```csharp
[TestClass]
public class WpsIntegrationTests
{
    [TestMethod]
    public async Task Monitor_ShouldDetectPasswordDialog()
    {
        var monitor = new WpsMonitor();
        var detected = false;
        
        monitor.PasswordDialogDetected += (s, e) => detected = true;
        monitor.Start();
        
        await SimulateWpsDialog();
        
        Assert.IsTrue(detected);
        monitor.Stop();
    }
}
```

## 11. 部署方案

### 11.1 安装包构�?

使用 .NET Publish �?Inno Setup 构建安装包：

```xml
<!-- PasswordManager.csproj -->
<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0-windows</TargetFramework>
    <PublishSingleFile>true</PublishSingleFile>
    <SelfContained>true</SelfContained>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
  </PropertyGroup>
</Project>
```

### 11.2 静默安装脚本

```batch
@echo off
msiexec /i PasswordManager.msi /qn /norestart
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v "PasswordManager" /t REG_SZ /d "\"%ProgramFiles%\PasswordManager\PasswordManager.exe\""
```

## 12. 参考规�?

### 12.1 代码规范

- 命名规范：PascalCase（类/方法）、camelCase（变�?参数�?
- 注释规范：XML 文档注释用于公共 API
- 文件编码：UTF-8 with BOM

### 12.2 安全规范

- OWASP Top 10
- Microsoft Secure Development Lifecycle
- NIST Cryptographic Standards

### 12.3 相关标准

- RFC 7519 - JSON Web Token
- RFC 7517 - JSON Web Key
- ECMA-376 - Office Open XML File Format

## 13. 附录

### A. 术语�?

| 术语 | 说明 |
|------|------|
| UI Automation | Windows UI 自动化框�?|
| NPOI | .NET 实现�?Office 文档处理�?|
| Open XML | Office 文档�?ZIP 包格�?|
| Qt Framework | 跨平�?C++ GUI 应用程序框架 |
| DPAPI | Windows Data Protection API |

### B. 参考资�?

- [.NET 6 Documentation](https://docs.microsoft.com/zh-cn/dotnet/)
- [Windows API Reference](https://docs.microsoft.com/zh-cn/windows/win32/api/)
- [NPOI GitHub Repository](https://github.com/nissl-lab/npoi)
- [UI Automation Documentation](https://docs.microsoft.com/zh-cn/dotnet/framework/ui-automation/)

### C. 版本历史

| 版本 | 日期 | 修改内容 |
|------|------|----------|
| v1.0 | 2024-01-01 | 初始版本发布 |
| v1.1 | 2024-XX-XX | 优化窗口识别算法 |

---

**文档版本**：v1.0  
**最后更�?*�?024�? 
**维护团队**：WPS Password Manager Team
